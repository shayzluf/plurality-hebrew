{% macro chapterVersionMenuItem(githubLink, dayStarted, yearStarted, monthStarted, language, contributeTranslated, pluralityTranslated,subtitleTranslated, pageTitle, pageUrl,chapterid_subid) %}

<p>
      {% if language=='english' %}

<div style="color:#26b72b; background:rgba(56,56,56,0.8) ; border-radius: 32px; padding:28px;gap:24px;margin-top:16px; margin-bottom; 16px;">
        <div id="sections_subsection">
            <span style="font-size:36px;">{{chapterid_subid}}</span>
            <span style="font-size:36px;">{{pageTitle}}</span>
            <span style="text-transform: none;"><a href="{{ pageUrl }}?mode=dark" class="button_small">Read in {{ language | capitalize }}</a></span>
            <span style="text-transform: none;width:48px;"><a href="{{ pageUrl }}?mode=light" class="button_small" style="width:20px;" title="Read in light mode">💡</a></span>
      {% if githubLink %}
        <span><a target="_blank" href="{{ githubLink }}">&#8599;&#xFE0E;</a></span>
      {% endif %}
        </div>
      {% else %}
        <div id="sections_subsection">
            <span style="font-size:36px;"></span>
            <span style="font-size:24px;font-family:Iansui">{{subtitleTranslated}}</span>
            <span style="text-transform: none;"><a href="{{ pageUrl }}?mode=dark" class="button_small">{{ language | capitalize }}</a></span>
            <span style="text-transform: none;width:48px;"><a href="{{ pageUrl }}?mode=light" class="button_small" style="width:20px;" title="Read in light mode">💡</a></span>
      {% if githubLink %}
        <span><a target="_blank" href="{{ githubLink }}">&#8599;&#xFE0E;</a></span>
      {% endif %}

        </div>
      {% endif %}
{% endmacro %}
